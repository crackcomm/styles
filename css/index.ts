/**
 * CSS Module.
 */
declare var require

const normalize: any = require('./normalize.css')
const particles: any = require('./particles.css')

export {
  normalize,
  particles,
  particles as $p,
}
